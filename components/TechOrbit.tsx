"use client";
import { useState } from "react";
import { technologies } from "@/lib/content";
export function TechOrbit(){const [active,setActive]=useState("n8n");return <div className="relative mt-14 min-h-[480px] glass rounded-[4px] overflow-hidden grid place-items-center p-6"><div className="absolute inset-0 grid-bg opacity-70"/><div className="absolute size-[370px] rounded-full border border-blue-400/15 animate-[spin_40s_linear_infinite]"/><div className="absolute size-[250px] rounded-full border border-violet-400/15 animate-[spin_30s_linear_infinite_reverse]"/><div className="relative z-10 text-center"><div className="size-28 mx-auto rounded-[4px] grid place-items-center bg-gradient-to-br from-blue-500/30 to-violet-500/20 border border-blue-300/25 shadow-[0_0_70px_#4f8cff30]"><span className="text-xl font-semibold">{active}</span></div><p className="text-sm text-slate-400 mt-5 max-w-sm">Selected for the job it does best—then integrated into a reliable, observable system.</p></div><div className="absolute inset-5 flex flex-wrap content-between justify-center gap-2 pointer-events-none">{technologies.map((t,i)=><button key={t} onMouseEnter={()=>setActive(t)} onFocus={()=>setActive(t)} className={`pointer-events-auto h-fit rounded-[4px] border px-3 py-2 text-xs backdrop-blur transition ${t===active?"border-blue-400 bg-blue-500/20 text-white":"border-white/10 bg-black/35 text-slate-400 hover:text-white"}`} style={{transform:`translateY(${Math.sin(i*1.9)*18}px)`}}>{t}</button>)}</div></div>}
