import crypto from "crypto";
export function encryptSecret(value:string){const raw=process.env.APP_ENCRYPTION_KEY;if(!raw||raw.length!==64)throw new Error("APP_ENCRYPTION_KEY must be 64 hexadecimal characters");const key=Buffer.from(raw,"hex"),iv=crypto.randomBytes(12),cipher=crypto.createCipheriv("aes-256-gcm",key,iv);const encrypted=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return [iv.toString("hex"),cipher.getAuthTag().toString("hex"),encrypted.toString("hex")].join(":")}
export function decryptSecret(value:string){const raw=process.env.APP_ENCRYPTION_KEY;if(!raw)return "";const [iv,tag,data]=value.split(":");const decipher=crypto.createDecipheriv("aes-256-gcm",Buffer.from(raw,"hex"),Buffer.from(iv,"hex"));decipher.setAuthTag(Buffer.from(tag,"hex"));return Buffer.concat([decipher.update(Buffer.from(data,"hex")),decipher.final()]).toString("utf8")}
